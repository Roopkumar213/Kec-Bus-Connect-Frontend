package com.kec.busconnect.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Branding & Accent Colors
val PrimaryBlue = Color(0xFF2563EB)        // Royal Blue 600
val PrimaryBlueDark = Color(0xFF1D4ED8)    // Blue 700
val PrimaryBlueLight = Color(0xFFEFF6FF)   // Blue 50
val PrimaryBlueBorder = Color(0xFFBFDBFE)  // Blue 200

val AccentAmber = Color(0xFFD97706)        // Amber 600
val AccentAmberLight = Color(0xFFFEF3C7)   // Amber 100
val SuccessEmerald = Color(0xFF059669)     // Emerald 600
val SuccessEmeraldLight = Color(0xFFD1FAE5)// Emerald 100
val DangerRose = Color(0xFFDC2626)         // Red 600
val DangerRoseLight = Color(0xFFFEE2E2)    // Red 100
val PurpleEvening = Color(0xFF7C3AED)      // Violet 600
val PurpleEveningLight = Color(0xFFEDE9FE) // Violet 100

// Light Theme Modern Clean Surfaces
val LightBackground = Color(0xFFF8FAFC)    // Slate 50 (clean soft background)
val LightSurface = Color(0xFFFFFFFF)       // Pure White (cards, modals)
val LightSurfaceVariant = Color(0xFFF1F5F9)// Slate 100
val LightCardBorder = Color(0xFFE2E8F0)    // Slate 200 (crisp modern border)

// High-Contrast Light Typography Colors
val TextPrimary = Color(0xFF0F172A)        // Slate 900 (crisp high contrast)
val TextSecondary = Color(0xFF475569)      // Slate 600
val TextMuted = Color(0xFF94A3B8)          // Slate 400

// Legacy Dark Tokens preserved for backward compatibility if needed
val DarkBackground = Color(0xFF0B1120)
val DarkSurface = Color(0xFF1E293B)
val DarkSurfaceVariant = Color(0xFF334155)
val DarkCardBorder = Color(0xFF334155)

