# Production (Vercel deployment) — points to Render backend:
VITE_API_URL=https://kec-bus-connect-backend.onrender.com/api

# Local development — uncomment and use this instead:
# VITE_API_URL=http://localhost:8081/api
